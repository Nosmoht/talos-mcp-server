---
generated_by: review-claude-config
schema_version: 1
date: 2026-04-06
target: /Users/thomaskrahn/workspace/talos-mcp
baseline_version: 2026-04-04
items_reviewed: 5
summary:
  - name: principal-architect-reviewer
    type: Agent
    path: .claude/agents/principal-architect-reviewer.md
    overall: A
    score: 91.0
    clarity: A
    completeness: B
    prompt_engineering: B
    context_engineering: A
    goal_alignment: A
    safety: A
    metadata: B
  - name: researcher
    type: Agent
    path: .claude/agents/researcher.md
    overall: A
    score: 93.5
    clarity: A
    completeness: B
    prompt_engineering: A
    context_engineering: A
    goal_alignment: A
    safety: A
    metadata: B
  - name: senior-implementer
    type: Agent
    path: .claude/agents/senior-implementer.md
    overall: A
    score: 88.0
    clarity: A
    completeness: B
    prompt_engineering: A
    context_engineering: B
    goal_alignment: A
    safety: B
    metadata: B
  - name: senior-plan-reviewer
    type: Agent
    path: .claude/agents/senior-plan-reviewer.md
    overall: A
    score: 93.5
    clarity: A
    completeness: B
    prompt_engineering: A
    context_engineering: A
    goal_alignment: A
    safety: A
    metadata: B
  - name: staff-reviewer
    type: Agent
    path: .claude/agents/staff-reviewer.md
    overall: A
    score: 92.5
    clarity: A
    completeness: B
    prompt_engineering: A
    context_engineering: A
    goal_alignment: A
    safety: A
    metadata: B
---

# Review Report — talos-mcp

**Generated by:** review-claude-config
**Date:** 2026-04-06
**Target:** `/Users/thomaskrahn/workspace/talos-mcp`
**Baseline version:** 2026-04-04
**Items reviewed:** 5 agents, 0 skills, 0 rules

---

## `.claude/agents/principal-architect-reviewer.md`

### Goal
Act as a procedural final-approval gate that verifies all prior review artifacts exist, are approved, and satisfy role-separation and scope-consistency requirements before allowing a commit to proceed.

### Certificate

| Dimension | Grade | Weight | Justification |
|-----------|-------|--------|---------------|
| Clarity | A | 15% | Five gate checks enumerated, deterministic, sequenced; output format fully specified. |
| Completeness | B | 15% | Happy path and failure modes documented; missing: resolution when `change-id` not supplied. |
| Prompt Engineering | B | 15% | Role priming, two `<example>` blocks with commentary, explicit constraints; minor gap: no constraint on `change-id` resolution. |
| Context Engineering | A | 15% | Minimal tool set (write/edit disabled), bounded scope, self-contained, no external file loads assumed. |
| Goal Alignment | A | 20% | Procedural-gatekeeper framing correct; all five governance invariants covered; explicit "do NOT re-evaluate" boundary. |
| Safety | A | 10% | Read-only enforced via frontmatter; defaults to `changes-requested` under uncertainty; no destructive actions possible. |
| Metadata | B | 10% | Frontmatter present, tool restrictions explicit, description clear; missing: `model` field (MS-1). |
| **Overall** | **A** | **100%** | **Weighted: 91.0** |

### Strengths
- Procedural-gatekeeper framing cleanly prevents scope creep — agent explicitly refuses to re-evaluate code quality.
- Two `<example>` blocks cover both the approval path and a representative rejection path with concrete YAML output.
- "Default to `changes-requested` under uncertainty" is a strong safety invariant that prevents false approvals.

### Recommendations

#### 1. Add `change-id` resolution instruction (Impact: Medium, Category: Prompt)

**Evidence:** Output format uses `<change-id>` as a template variable and all gate checks reference `.claude/reviews/<change-id>/`, but the body never specifies how the agent obtains this value.

**Why it matters:** If two concurrent changes have open reviews or the caller omits `change-id`, no deterministic resolution strategy exists. Two instances would behave differently from the same ambiguous input.

**Validation:** Invoke the agent without a `change-id` argument and confirm it produces `changes-requested` rather than scanning or guessing.

**Current:**
```
Produce a review artifact at `.claude/reviews/<change-id>/final-approval.md`
```

**Recommended:**
```
## Input Requirements

The caller must supply `change-id` as the first argument (e.g., `add-etcd-alarms-tool`).
If `change-id` is absent or ambiguous, halt immediately with:
  status: changes-requested
  finding: "change-id not supplied — cannot locate review artifacts"
Do not infer or scan for a change-id.
```

#### 2. Add `model` field to frontmatter (Impact: Low, Category: Metadata)

**Evidence:** MS-1 FAIL — no `model:` key. Task is deterministic file inspection (low-medium complexity).

**Why it matters:** Without explicit model declaration, the dispatcher inherits the caller's model context, which may be over-provisioned for what is essentially a deterministic artifact-checking task.

**Validation:** Confirm after adding that a test invocation uses the declared model rather than the parent session model.

**Current:** *(no `model` field in frontmatter)*

**Recommended:** `model: claude-haiku-4-5` — task is deterministic artifact-checking.

---

## `.claude/agents/researcher.md`

### Goal
Resolve implementation and review uncertainty by searching the local repository first, then official documentation, and returning a structured, source-cited research memo without modifying any files.

### Certificate

| Dimension | Grade | Weight | Justification |
|-----------|-------|--------|---------------|
| Clarity | A | 15% | Sequential protocol (repo → docs → cite → state confidence) explicit and deterministic; output format fully specified. |
| Completeness | B | 15% | Happy path and confidence levels documented; no explicit failure mode for "nothing found anywhere." |
| Prompt Engineering | A | 15% | Role priming, two `<example>` blocks covering high- and medium-confidence outcomes, explicit constraints, verification criteria. |
| Context Engineering | A | 15% | Minimal tool set (write/edit disabled); no unnecessary pre-loaded context; JIT retrieval via protocol steps. |
| Goal Alignment | A | 20% | Repo-first, official-doc fallback, citation requirement, honest uncertainty align precisely with the mission; domain targets named explicitly. |
| Safety | A | 10% | write:false and edit:false in frontmatter; read-only constraint restated in body; no-implementation-decisions guardrail prevents scope creep. |
| Metadata | B | 10% | Description specific and well-scoped; tool declarations correct; no `model` field (MS-1). |
| **Overall** | **A** | **100%** | **Weighted: 93.5** |

### Strengths
- Read-only enforcement applied at two levels (frontmatter flags + explicit body constraint).
- Two examples are complementary: repo-sufficient (high confidence) vs. repo-insufficient with web escalation (medium confidence).
- Output format mandates both confidence tier and recommendation, closing the loop for the consumer agent.

### Recommendations

#### 1. Document the "nothing found" failure path (Impact: Medium, Category: Completeness)

**Evidence:** TC-3 — no output template or protocol step covers the case where both repo search and official docs return no conclusive results.

**Why it matters:** Without a defined failure output, a consumer agent may receive a low-confidence memo structurally identical to a resolved one, causing silent downstream errors (arXiv:2602.17753 — incomplete inputs flagged explicitly per artifact is a top-3 reliability gap).

**Validation:** Simulate an "unknown API" query and confirm a `none`-confidence memo is produced rather than speculation.

**Current:** Protocol ends at step 4 ("State confidence"); no prescribed behavior for irreducibly low confidence.

**Recommended:**
```
5. If no grounding found after repo + official docs, emit `Confidence: none` and list
   what was searched. Do not speculate.
   When Confidence is `none`, Recommendation must read:
   "Escalate to human — insufficient evidence."
```

#### 2. Add `model` field to frontmatter (Impact: Low, Category: Metadata)

**Evidence:** MS-1 FAIL — no `model:` key.

**Why it matters:** Research tasks require strong retrieval and synthesis reasoning; if the environment default is a lighter model, quality degrades silently.

**Validation:** Confirm correct model invoked after adding the field.

**Current:** No `model:` key in frontmatter.

**Recommended:** `model: claude-sonnet-4-5`

---

## `.claude/agents/senior-implementer.md`

### Goal
Implement Go code changes for the talos-mcp MCP server while enforcing role separation and preventing self-review.

### Certificate

| Dimension | Grade | Weight | Justification |
|-----------|-------|--------|---------------|
| Clarity | A | 15% | Sequential workflow explicit: implement → `make check` → produce handoff → request review; all conditionals deterministic. |
| Completeness | B | 15% | Happy path, ambiguity handling, CI failure paths covered; missing: what to do if reviewer unavailable or artifacts malformed. |
| Prompt Engineering | A | 15% | Role priming, positive + negative few-shot examples, explicit constraints, verification criteria (`make check`), structured handoff output. |
| Context Engineering | B | 15% | Tool set minimal (write + edit only); agent cannot run `make check` as written — `bash` missing despite body referencing it. |
| Goal Alignment | A | 20% | Deep domain knowledge embedded (handler signatures, error wrapping, audit logging, build flags, linter list); tied to observable codebase patterns. |
| Safety | B | 15% | Self-review prohibition explicit with example; commit gate on artifacts present; missing: no pre-write confirmation gate. |
| Metadata | B | 5% | Required fields present; description accurate; `bash` not listed but body references `make check` — one tool mismatch. |
| **Overall** | **A** | **100%** | **Weighted: 88.0** |

### Strengths
- Self-review prevention enforced at two levels: explicit constraint + negative few-shot example modeling the refusal.
- Project conventions section is dense and precise — enough domain grounding to produce idiomatic code without external lookup.
- Structured handoff format creates a deterministic artifact that downstream reviewer agents can parse.

### Recommendations

#### 1. Add `bash` to tool list or remove `make check` from workflow (Impact: High, Category: Context)

**Evidence:** Body instructs "fix all issues before producing the handoff" after `make check` fails, but `bash` is absent from `tools`. An agent without `bash` cannot execute shell commands — this is a structurally unreachable dead branch.

**Why it matters:** When the agent cannot run `make check`, it either silently skips the gate or halts without a clear error — both defeat the CI gate's purpose.

**Validation:** Invoke on a change with a deliberate lint error; confirm the agent runs `make check`, catches the failure, and self-corrects before handoff.

**Current:**
```yaml
tools:
  write: true
  edit: true
```

**Recommended:** Either add `bash: true` scoped to project root, or replace the `make check` instruction with:
```
Record that CI validation is deferred to the reviewer if bash access is intentionally withheld.
```

#### 2. Add a pre-write confirmation gate (Impact: Medium, Category: Safety)

**Evidence:** Domain best practices — "Gate before Write on production paths; unresolved path tokens are a silent failure class." Agent has `write: true` with no pre-write self-verification step.

**Why it matters:** A misread requirement produces a write that must be manually reverted; without a gate the agent may modify files before the user has seen the plan.

**Validation:** Invoke with an ambiguous requirement; confirm the agent surfaces the intended file list for confirmation before writing.

**Current:** No confirmation step between requirement receipt and file write.

**Recommended:**
```
Before writing any file, output the list of files to be created or modified
and their intended change summary. Proceed only after confirming this matches
the accepted plan.
```

#### 3. Clarify the `researcher` escalation threshold (Impact: Medium, Category: Prompt)

**Evidence:** "If requirements are ambiguous, invoke the `researcher` agent" — no criteria for what constitutes "ambiguous" or what output is sufficient to resume.

**Why it matters:** Without a threshold, the agent may over-escalate (stalling on trivialities) or under-escalate (proceeding on genuinely unclear specs) — top-3 reliability gap (arXiv:2602.17753, Tier 1).

**Validation:** Present an edge-case requirement and confirm the agent correctly classifies it as ambiguous vs. resolvable.

**Current:**
```
If requirements are ambiguous, invoke the `researcher` agent before proceeding.
```

**Recommended:**
```
Ambiguous means: the target file or function cannot be inferred, or the required
behavior contradicts an existing convention. If only implementation detail is
unclear, apply the most conservative existing pattern and note the assumption
in the handoff.
```

---

## `.claude/agents/senior-plan-reviewer.md`

### Goal
Review proposed implementation plans for completeness, risk, and architectural alignment before any code is written in the talos-mcp project.

### Certificate

| Dimension | Grade | Weight | Justification |
|-----------|-------|--------|---------------|
| Clarity | A | 15% | Sequential workflow explicit; no ambiguous conditionals; status rule binary and unambiguous. |
| Completeness | B | 15% | Happy/rejection paths defined with output schema; failure modes documented; gap: no instruction for plans with no change-id slug. |
| Prompt Engineering | A | 15% | Role priming, two few-shot examples (pass and fail), explicit constraints, clear verification criterion, natural instruction calibration. |
| Context Engineering | A | 15% | Minimal tool set (write/edit disabled); Grep/Read permitted for file verification; self-contained. |
| Goal Alignment | A | 20% | Heuristics map to plan-review concerns; talos-mcp package boundaries named explicitly; output artifact fully specified. |
| Safety | A | 10% | write:false, edit:false in frontmatter; read-only posture in description; no ability to modify reviewed files. |
| Metadata | B | 10% | Frontmatter complete; tool list matches; temperature appropriate; minor gap: no `name` field. |
| **Overall** | **A** | **100%** | **Weighted: 93.5** |

### Strengths
- Binary status rule (zero findings = approved; any finding = changes-requested) eliminates hedged approvals.
- Two `<example>` blocks with `<commentary>` tags are well-chosen — approval + specific rejection with actionable fix.
- Failure modes proactively handle the "vague plan" and "unverifiable file reference" traps.

### Recommendations

#### 1. Add failure mode for missing `change-id` (Impact: Medium, Category: Completeness)

**Evidence:** Output format says `<semantic-slug from the plan>` with no fallback. Incomplete inputs must be flagged explicitly per artifact (arXiv:2602.17753, Tier 1).

**Why it matters:** If the agent invents a change-id, the artifact lands at an unpredictable path, breaking the audit-fix chain and downstream apply flows.

**Validation:** Submit a plan with no semantic slug; confirm the agent flags it rather than proceeding silently.

**Current:** `change-id: <semantic-slug from the plan>` with no fallback instruction.

**Recommended:** Add to Failure Modes:
```
- If the plan contains no semantic slug, that is a critical finding:
  "Plan is missing a change-id slug; provide one before review can proceed."
  Do not write an artifact with an invented slug.
```

#### 2. Add `name` field to frontmatter (Impact: Low, Category: Metadata)

**Evidence:** Frontmatter has `temperature`, `description`, `tools` only — no `name` key.

**Why it matters:** `name` is the canonical display identifier used by analytics and apply flows. Its absence may cause check-repo-health warnings.

**Validation:** Run `/check-repo-health integrity` after adding; confirm no metadata warnings for this agent.

**Current:** *(no `name` field)*

**Recommended:** Add `name: senior-plan-reviewer` as the first frontmatter field.

---

## `.claude/agents/staff-reviewer.md`

### Goal
Review completed Go implementation artifacts for correctness, idiom compliance, test coverage, documentation accuracy, and security, producing a structured review artifact with explicit pass/fail status.

### Certificate

| Dimension | Grade | Weight | Justification |
|-----------|-------|--------|---------------|
| Clarity | A | 15% | Sequential workflow explicit; status rule deterministic with no ambiguity. |
| Completeness | B | 15% | Happy/rejection paths defined; failure modes cover missing tests and missing plan artifact; no negative trigger example. |
| Prompt Engineering | A | 15% | Role priming, two concrete `<example>` blocks with file:line evidence, explicit constraints, evidence-first critique enforced. |
| Context Engineering | A | 15% | write:false/edit:false enforce read-only; no unnecessary tool provisioning; body self-contained. |
| Goal Alignment | A | 20% | Covers all Go staff-review concerns: correctness, idioms, tests, documentation, security, CI readiness; scope boundary with principal-architect-reviewer explicit. |
| Safety | A | 10% | Least-privilege enforced; plan-artifact absence treated as critical finding; no self-approval path. |
| Metadata | B | 10% | Frontmatter complete; description accurate; no `model` field (MS-1). |
| **Overall** | **A** | **100%** | **Weighted: 92.5** |

### Strengths
- `<example>` blocks are exceptionally concrete — full YAML output for approval and specific file:line finding for rejection.
- Scope boundary ("You do NOT perform governance checks") prevents role bleed and keeps decision surface narrow.
- Failure modes proactively handle the two most common silent-skip traps: missing test files and missing plan artifacts, both critical.

### Recommendations

#### 1. Add `model` field to frontmatter (Impact: Medium, Category: Metadata)

**Evidence:** MS-1 FAIL — `model` absent from frontmatter.

**Why it matters:** Staff-level code review benefits from a high-capability model; silent inheritance is a misconfiguration risk as defaults change over time.

**Validation:** Confirm the agent activates with the intended model after adding the field.

**Current:** `temperature: 0.1` with no `model:` key.

**Recommended:**
```yaml
model: claude-sonnet-4-5
```

#### 2. Add a negative trigger example (Impact: Low, Category: Examples)

**Evidence:** TC-2 FAIL — no example for when NOT to trigger. Without one, this agent may be invoked on plan documents rather than `senior-plan-reviewer`.

**Why it matters:** Spurious invocation on plan artifacts produces irrelevant Go-idiom findings on non-code content.

**Validation:** After adding, confirm the activation boundary is unambiguous between this agent and `senior-plan-reviewer`.

**Current:** Two positive `<example>` blocks only.

**Recommended:** Add a third `<example>` block with a plan document as input and a note that plans go to `senior-plan-reviewer`, not `staff-reviewer`.

---

## Summary

| Item | Type | Overall | Clarity | Completeness | PE | CE | Goal | Safety | Meta |
|------|------|---------|---------|--------------|----|----|------|--------|------|
| principal-architect-reviewer | Agent | **A** (91.0) | A | B | B | A | A | A | B |
| researcher | Agent | **A** (93.5) | A | B | A | A | A | A | B |
| senior-implementer | Agent | **A** (88.0) | A | B | A | B | A | B | B |
| senior-plan-reviewer | Agent | **A** (93.5) | A | B | A | A | A | A | B |
| staff-reviewer | Agent | **A** (92.5) | A | B | A | A | A | A | B |

---

## Cross-Cutting Observations

**Consistent strength — binary status rules:** All four reviewer agents implement a strict zero-findings = approved rule with no hedged middle state. This is the correct pattern for agentic governance workflows and should be preserved in any future agents.

**Consistent strength — role separation examples:** The `senior-implementer` and `principal-architect-reviewer` both use negative few-shot examples to model self-review refusal. This dual-layer enforcement (constraint + example) is robust against prompt drift.

**Systemic gap — missing `model` field across all agents:** Four of five agents (`researcher`, `principal-architect-reviewer`, `staff-reviewer`, `senior-plan-reviewer`) lack a `model:` frontmatter key (MS-1). All are subject to environment-default drift. Adding `model: claude-sonnet-4-5` (or `claude-haiku-4-5` for the purely procedural `principal-architect-reviewer`) to all four is a low-effort, high-reliability fix.

  Example: `principal-architect-reviewer.md`, `researcher.md`, `staff-reviewer.md`, `senior-plan-reviewer.md` — all missing `model:` key.

**Systemic gap — `change-id` resolution undefined in two agents:** Both `principal-architect-reviewer` and `senior-plan-reviewer` use `<change-id>` as a template variable without specifying how to obtain it or what to do when absent. Concurrent changes or caller omission will cause unpredictable artifact paths.

  Example: `principal-architect-reviewer.md` — "Produce a review artifact at `.claude/reviews/<change-id>/final-approval.md`" with no input requirements section.

**One structural concern — senior-implementer `bash` gap:** The `senior-implementer` instructs `make check` execution but has no `bash` tool. This is the only agent where a workflow step is structurally unreachable given the declared tool set. Fix: add `bash: true` or explicitly delegate CI validation to the reviewer chain.

  Example: `senior-implementer.md` — `tools: write: true, edit: true` but body references `make check` execution.
